<h1>Editing accounting_entry</h1>

<%= error_messages_for :accounting_entry %>

<% form_for(:accounting_entry, :url => entry_path(@accounting_entry), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', entry_path(@accounting_entry) %> |
<%= link_to 'Back', entries_path %>