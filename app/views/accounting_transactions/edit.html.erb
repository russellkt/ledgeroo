<h1>Editing accounting_transaction</h1>

<%= error_messages_for :accounting_transaction %>

<% form_for(:accounting_transaction, :url => transaction_path(@accounting_transaction), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', transaction_path(@accounting_transaction) %> |
<%= link_to 'Back', transactions_path %>